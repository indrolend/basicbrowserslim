import{SPA_SECTIONS,DESKTOP_CHAIN_WINDOW_MS,getSection}from'./spaData.js';export function getAvailableSections(h){return h?SPA_SECTIONS.filter((_,i)=>i!==0):SPA_SECTIONS}export function getNextTarget(s,i,h){const sec=getSection(s);if(!sec)return null;if(i+1<sec.items.length)return{sectionIdx:s,itemIdx:i+1};const a=getAvailableSections(h);const p=a.findIndex(x=>x===sec);const n=a[(p+1)%a.length];return{sectionIdx:SPA_SECTIONS.indexOf(n),itemIdx:0}}export function getPrevTarget(s,i,h){const sec=getSection(s);if(!sec)return null;if(i-1>=0)return{sectionIdx:s,itemIdx:i-1};const a=getAvailableSections(h);const p=a.findIndex(x=>x===sec);const pr=a[(p-1+a.length)%a.length];const ps=SPA_SECTIONS.indexOf(pr);return{sectionIdx:ps,itemIdx:SPA_SECTIONS[ps].items.length-1}}export function getTargetForDirection(d,s,i,h){return d==='next'?getNextTarget(s,i,h):getPrevTarget(s,i,h)}export function createDesktopNavTracker(){let l=0;return{getNavOptions(){const n=performance.now();const c=(n-l)<DESKTOP_CHAIN_WINDOW_MS;l=n;return{timingProfile:c?'chained':'default'}}}}
