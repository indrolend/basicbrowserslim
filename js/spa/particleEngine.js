export function runParticleAnimation(ctx,plan,onComplete){const{particles,phases}=plan;const w=ctx.canvas.width;const h=ctx.canvas.height;let done=false;function safeComplete(){if(done)return;done=true;if(typeof onComplete==='function')onComplete()}if(!particles.length||!phases.length){safeComplete();return}const pO=[];let offset=0;for(const phase of phases){pO.push(offset);offset+=phase.duration}const tD=offset;let sT=null;function animate(ts){if(!ctx.canvas.isConnected){safeComplete();return}if(sT===null)sT=ts;const el=ts-sT;if(el>=tD){safeComplete();return}ctx.clearRect(0,0,w,h);let pi=0;while(pi+1<phases.length&&el>=pO[pi+1])pi++;phases[pi].tick(el-pO[pi],particles,ctx);requestAnimationFrame(animate)}requestAnimationFrame(animate)}
