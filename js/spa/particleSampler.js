export const PARTICLE_SIZE=4;let _sc=null;export function sampleParticles(reg,cW,cH){if(!_sc)_sc=document.createElement('canvas');const c=_sc;if(c.width!==cW||c.height!==cH){c.width=cW;c.height=cH}const cc=c.getContext('2d');const dx=(cW-reg.width)/2;const dy=(cH-reg.height)/2;cc.clearRect(0,0,cW,cH);cc.drawImage(reg.canvas,0,0,reg.width,reg.height,dx,dy,reg.width,reg.height);const iD=cc.getImageData(0,0,cW,cH).data;const res=[];for(let y=0;y<cH;y+=PARTICLE_SIZE){for(let x=0;x<cW;x+=PARTICLE_SIZE){const idx=(y*cW+x)*4;const r=iD[idx],g=iD[idx+1],b=iD[idx+2],a=iD[idx+3];if(a>32)res.push({x,y,color:`rgba(${r},${g},${b},${a/255})`})}}return res}export function sampleByCoverage(lst,cnt){if(!lst.length||cnt<=0)return[];const out=[];for(let i=0;i<cnt;i++){out.push(lst[Math.floor((i*lst.length)/cnt)%lst.length])}return out}export function shuffle(lst){const arr=lst.slice();for(let i=arr.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[arr[i],arr[j]]=[arr[j],arr[i]]}return arr}export function parseRgba(color){const m=/rgba\((\d+),(\d+),(\d+),([0-9.]+)\)/.exec(color);return m?[+m[1],+m[2],+m[3],+m[4]]:[255,255,255,1]}
